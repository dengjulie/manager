provider tag - must give an instance of a redux store, what connects to all those different connect tags that we scatter about our application, makes sure that all those connect tags can get access to the store grab our redux state and pass it off to individual components

npm install --save firebase

don't need to, but we choose to add redux to log in form bc app will grow in complexity 
